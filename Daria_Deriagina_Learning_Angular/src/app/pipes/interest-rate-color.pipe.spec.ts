import { InterestRateColorPipe } from './interest-rate-color.pipe';

describe('InterestRateColorPipe', () => {
  it('create an instance', () => {
    const pipe = new InterestRateColorPipe();
    expect(pipe).toBeTruthy();
  });
});
